#! /usr/bin/env python
# -*- coding: utf-8 -*-
from __future__ import (absolute_import, division, print_function, unicode_literals)

# grabs the folder where the script runs
# import os
# basedir = os.path.abspath(os.path.dirname(__file__))


# configuration
import logging

DATABASE = 'custeio.db'
DEBUG = True
SECRET_KEY = 'my_precious'
USERNAME = 'admin'
PASSWORD = 'admin'
LOG_LEVEL = logging.INFO
LOG_FORMAT = logging.Formatter('%(asctime)s - %(name)s / %(module)s / %(funcName)s - %(levelname)s - %(message)s')
